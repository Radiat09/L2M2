"use strict";
{
    const user1 = {
        name: "John",
        age: 30,
        role: "admin",
    };
    //   const rollNumber: Roll1 = [1, 2, 3, 4, 5]; // array of numbers`
    const rollNumber = [1, 2, 3, 4, 5]; // array of numbers`
    //   const add: Add = (num1, num2) => num1 + num2; // function type
    const add = (num1, num2) => num1 + num2;
}
